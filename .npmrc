# Allow build scripts for required native dependencies
enable-pre-post-scripts=true

# Approve specific packages that need to run build scripts
# These are required for: image optimization (sharp), file watching, Firebase, etc.
build-trust=@firebase/util,@parcel/watcher,protobufjs,sharp,unrs-resolver
